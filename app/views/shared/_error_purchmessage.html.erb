<% if @purch.errors.any? %>
<div >
   
     The form contains <%= pluralize(@purch.errors.count, 'error') %>
     </div>
     <ul>
     <% @purch.errors.full_messages.each do |e| %>
      <li> <%=e %></li>
      <% end %>
      </ul>
  
<% end %> 